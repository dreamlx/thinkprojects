<h1>New dict</h1>

<% @dict.errors.full_messages.each do |msg| %>
  <p><%= msg %></p>
<% end %>
<%= form_for(:dict, :url => dicts_path) do |f| %>

  <%= render :partial => 'form',:locals => { :f => f }%>

  <p>
    <%= submit_tag 'Submit' %>
  </p>
<% end %>

<%= link_to 'Back', dicts_path %>
