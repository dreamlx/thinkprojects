<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title><%=h(yield(:title)||controller.action_name)%></title>
    <%= stylesheet_link_tag 'application'%>
    <%= stylesheet_link_tag 'tablecloth','dropdown','themes','tcal' %>
    <%= javascript_include_tag :defaults,'webCalender','tcal'%>

    <%= yield(:head)%>
  </head>
  <body>
    <div class="container">
      <div id="user_bar">
        <%= render "layouts/user_bar"%>
      </div>

      <div id="top_menu">
        <%= render 'layouts/topmenu' %>
      </div>

      <p class="notice"><%= notice %></p>
      <p class="alert"><%= alert %></p>

      <%= yield %>
    </div>
  </body>
</html>
