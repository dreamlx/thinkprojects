<% Industry.content_columns.each do |column| %>
<p>
  <b><%= column.human_name %>:</b> <%= @industry.send(column.name) %>
</p>
<% end %>

<%= link_to 'Edit', edit_industry_path(@industry) %> |
<%= link_to 'Back', industries_path %>
