<tr>
  <td><%= client.chinese_name %></td>
  <td><%= client.english_name %></td>
  <td><b><%= client.client_code %></b></td>
  <td><%= client.user.english_name if client.user %></td>
  <td><%= link_to t('show'), client %></td>
  <td><%= link_to t('edit'), edit_client_path(client) %></td>
  <td><%= link_to t('destroy'), client, confirm: t('are_you_sure'), method: :delete %></td>
</tr>