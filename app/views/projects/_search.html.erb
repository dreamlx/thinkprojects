<%= simple_form_for(:project, :url => projects_path, :html => { :method => :get }) do |f| %>
  <%= f.input :job_code, required: false %>
  <%= f.input :client_name, required: false %>
  <%#= f.select("booking_id", Person.my_teams(current_user).collect {|p| [ p.english_name, p.id ] }, { :include_blank => "All" }) %>
  <%= f.input :state, collection: ["pending","approved","disapproved","closed"], required: false %>
  <%= f.input :order_by, collection: ["job_code","created_on","client_name"] , required: false%>
  <%= f.button :submit, '查询' %>
<% end %>