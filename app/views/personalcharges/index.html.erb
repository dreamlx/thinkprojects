<%= form_for(:personalcharge, :url => personalcharges_path, :html => { :method => :get }) do |f| %>
  <%= render :partial => "search" %>
<% end %>
<%#= link_to 'Export data', :controller=>"reports", :action => "personalcharges_export"  %>||
<%= link_to 'New TimeCost', :action => 'new', :id=>params[:id], :person_id => params[:person_id]  %>

<form>
  <%= render 'shared/all_op' %>
</form>
<table  class="table table-bordered">
  <tr>
  	<th>Employee name</th>  
	  <th>Period</th>
    <th>Charge date</th>
    <th>Job code</th>
    <th>Hours</th>
    <th>Including OT hours</th>
    <th>Description</th>
    <th>State</th>
    <th colspan="4">Operations</th>
  </tr>
  <% sum_current_person_daily_hours = 0 %>
  <%= render @personalcharges %>
</table>

<%= button_to "Get OverTime", :url=> {:action=> "get_ot",:approved_personalcharges=> @approved_personalcharges, :effective_hours => @effective_hours}, :method =>:post,:update => "the-ot" %>

<div id="the-ot"></div>

<%= will_paginate @personalcharges %>