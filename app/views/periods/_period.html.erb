<tr>
  <td><%= period.number %></td>
  <td><%= period.work_hours %></td>
  <td><%= period.starting_date %></td>
  <td><%= period.ending_date %></td>
  <td><%= link_to t('show'), period %></td>
  <td><%= link_to t('edit'), edit_period_path(period) %></td>
  <td><%= link_to t('destroy'), period, :confirm => t('are_you_sure'), :method => :delete %></td>
</tr>